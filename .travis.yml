language: go
go:
  - 1.4
  - 1.5

sudo: required

cache:
  apt: true
  directories:
    - $HOME/librtlsdr

addons:
  apt:
    packages:
    - libusb-1.0-0-dev

before_install:
  - bash install-rtlsdr.sh